[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"

# SPA fallback: all routes are handled by React Router.
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
